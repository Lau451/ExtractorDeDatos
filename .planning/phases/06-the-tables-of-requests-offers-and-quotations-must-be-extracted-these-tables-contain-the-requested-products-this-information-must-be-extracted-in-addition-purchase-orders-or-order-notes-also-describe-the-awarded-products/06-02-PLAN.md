---
phase: 06-product-table-extraction
plan: 02
type: execute
wave: 1
depends_on: []  # Intentionally parallel with Plan 01 — files are fully disjoint (Python backend vs TypeScript frontend)
files_modified:
  - frontend/src/lib/fieldLabels.ts
  - frontend/src/lib/docTypes.ts
  - frontend/src/components/LineItemsTable.test.tsx
autonomous: true
requirements:
  - P6-FE-01
  - P6-FE-02

must_haves:
  truths:
    - "Frontend renders LineItemsTable for tender_rfq documents"
    - "Frontend renders LineItemsTable for quotation documents"
    - "LINE_ITEM_KEYS includes tender_rfq and quotation entries"
    - "DOC_TYPES_WITH_LINE_ITEMS includes tender_rfq and quotation"
    - "Automated tests prove tender_rfq and quotation are in LINE_ITEM_KEYS and DOC_TYPES_WITH_LINE_ITEMS"
  artifacts:
    - path: "frontend/src/lib/fieldLabels.ts"
      provides: "LINE_ITEM_KEYS with tender_rfq and quotation entries"
      contains: "tender_rfq: 'line_items'"
    - path: "frontend/src/lib/docTypes.ts"
      provides: "DOC_TYPES_WITH_LINE_ITEMS with tender_rfq and quotation"
      contains: "'tender_rfq'"
    - path: "frontend/src/components/LineItemsTable.test.tsx"
      provides: "Tests proving P6-FE-01 and P6-FE-02 rendering requirements"
      contains: "tender_rfq"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/lib/fieldLabels.ts"
      via: "LINE_ITEM_KEYS[jobData.doc_type]"
      pattern: "LINE_ITEM_KEYS"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/lib/docTypes.ts"
      via: "DOC_TYPES_WITH_LINE_ITEMS.has(jobData.doc_type)"
      pattern: "DOC_TYPES_WITH_LINE_ITEMS"
---

<objective>
Extend frontend constants so that Tender/RFQ and Quotation documents display the LineItemsTable component in the review screen. Create automated tests proving the constants are correctly configured.

Purpose: Without these constant updates, the frontend will not render the line items table for tender/quotation docs even after the backend extracts them. Both `LINE_ITEM_KEYS` and `DOC_TYPES_WITH_LINE_ITEMS` must be updated — they serve as the routing guard in App.tsx. Tests ensure P6-FE-01 and P6-FE-02 are non-vacuously verified by `npx vitest run`.

Note: This plan is intentionally parallel with Plan 01 (wave 1, no depends_on) because their file sets are fully disjoint — Plan 01 modifies Python backend files while this plan modifies TypeScript frontend files. No shared artifacts or ordering constraints exist.

Output: Two updated TypeScript files, one new test file.
</objective>

<execution_context>
@C:/Users/LAUREANO/OneDrive/Escritorio/ExtraccionDeDatosNE2.0/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LAUREANO/OneDrive/Escritorio/ExtraccionDeDatosNE2.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current state of both files — executor must update in-place -->

From frontend/src/lib/fieldLabels.ts (line 71-75):
```typescript
export const LINE_ITEM_KEYS: Record<string, string> = {
  purchase_order: 'line_items',
  invoice: 'line_items',
  supplier_comparison: 'line_items',
};
```

From frontend/src/lib/docTypes.ts (line 1-5):
```typescript
export const DOC_TYPES_WITH_LINE_ITEMS = new Set([
  'purchase_order',
  'invoice',
  'supplier_comparison',
]);
```

From frontend/src/App.tsx (usage pattern):
```typescript
// LINE_ITEM_KEYS[jobData.doc_type] determines lineItemKey for ReviewTable
// DOC_TYPES_WITH_LINE_ITEMS.has(jobData.doc_type) guards LineItemsTable rendering
```

Existing test pattern (from frontend/src/components/ReviewTable.test.tsx):
```typescript
import { render, screen } from '@testing-library/react';
import { describe, it, expect } from 'vitest';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create LineItemsTable test file (Wave 0 — Nyquist)</name>
  <files>frontend/src/components/LineItemsTable.test.tsx</files>
  <action>
Create `frontend/src/components/LineItemsTable.test.tsx` with tests that verify the constants are configured for tender_rfq and quotation. This file must exist BEFORE Task 1 runs so that `npx vitest run` is non-vacuous for P6-FE-01 and P6-FE-02.

Write the following test file:

```typescript
import { describe, it, expect } from 'vitest';
import { LINE_ITEM_KEYS } from '../lib/fieldLabels';
import { DOC_TYPES_WITH_LINE_ITEMS } from '../lib/docTypes';

describe('LineItemsTable constants for tender_rfq and quotation', () => {
  it('LINE_ITEM_KEYS includes tender_rfq mapped to line_items (P6-FE-01)', () => {
    expect(LINE_ITEM_KEYS['tender_rfq']).toBe('line_items');
  });

  it('DOC_TYPES_WITH_LINE_ITEMS includes tender_rfq (P6-FE-01)', () => {
    expect(DOC_TYPES_WITH_LINE_ITEMS.has('tender_rfq')).toBe(true);
  });

  it('LINE_ITEM_KEYS includes quotation mapped to line_items (P6-FE-02)', () => {
    expect(LINE_ITEM_KEYS['quotation']).toBe('line_items');
  });

  it('DOC_TYPES_WITH_LINE_ITEMS includes quotation (P6-FE-02)', () => {
    expect(DOC_TYPES_WITH_LINE_ITEMS.has('quotation')).toBe(true);
  });
});
```

These tests will FAIL initially (RED) because fieldLabels.ts and docTypes.ts do not yet contain tender_rfq or quotation entries. Task 1 will make them pass (GREEN).
  </action>
  <verify>
    <automated>cd "C:/Users/LAUREANO/OneDrive/Escritorio/ExtraccionDeDatosNE2.0/frontend" && npx vitest run src/components/LineItemsTable.test.tsx --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>Test file exists at frontend/src/components/LineItemsTable.test.tsx with 4 test cases covering P6-FE-01 and P6-FE-02. Tests will fail until Task 1 updates the constants (expected RED phase).</done>
</task>

<task type="auto">
  <name>Task 1: Update frontend LINE_ITEM_KEYS and DOC_TYPES_WITH_LINE_ITEMS</name>
  <files>frontend/src/lib/fieldLabels.ts, frontend/src/lib/docTypes.ts</files>
  <read_first>
    frontend/src/lib/fieldLabels.ts
    frontend/src/lib/docTypes.ts
    frontend/src/App.tsx
  </read_first>
  <action>
1. In `frontend/src/lib/fieldLabels.ts`, update the `LINE_ITEM_KEYS` constant (around line 71-75) to add two new entries:

```typescript
export const LINE_ITEM_KEYS: Record<string, string> = {
  purchase_order: 'line_items',
  invoice: 'line_items',
  supplier_comparison: 'line_items',
  tender_rfq: 'line_items',
  quotation: 'line_items',
};
```

2. In `frontend/src/lib/docTypes.ts`, update the `DOC_TYPES_WITH_LINE_ITEMS` constant (around line 1-5) to add two new entries:

```typescript
export const DOC_TYPES_WITH_LINE_ITEMS = new Set([
  'purchase_order',
  'invoice',
  'supplier_comparison',
  'tender_rfq',
  'quotation',
]);
```

No other files need changes. The existing `LineItemsTable` component is generic — it renders any fields found in the items array. The existing `FIELD_LABELS` in fieldLabels.ts already contains labels for `item_number` ('Item #'), `quantity` ('Quantity'), and `description` ('Description'), so no new labels are needed.
  </action>
  <verify>
    <automated>cd "C:/Users/LAUREANO/OneDrive/Escritorio/ExtraccionDeDatosNE2.0/frontend" && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - fieldLabels.ts LINE_ITEM_KEYS contains `tender_rfq: 'line_items'`
    - fieldLabels.ts LINE_ITEM_KEYS contains `quotation: 'line_items'`
    - docTypes.ts DOC_TYPES_WITH_LINE_ITEMS Set contains `'tender_rfq'`
    - docTypes.ts DOC_TYPES_WITH_LINE_ITEMS Set contains `'quotation'`
    - `npx vitest run` exits 0 with all tests passing (including the 4 new LineItemsTable tests from Task 0)
  </acceptance_criteria>
  <done>Both frontend constants updated. tender_rfq and quotation docs will now render LineItemsTable in the review screen. All 4 LineItemsTable tests pass (GREEN). No component changes needed — existing LineItemsTable is generic.</done>
</task>

</tasks>

<verification>
```bash
# Frontend tests pass (including new LineItemsTable tests)
cd frontend && npx vitest run --reporter=verbose

# Verify constants contain new entries
grep -n "tender_rfq" frontend/src/lib/fieldLabels.ts frontend/src/lib/docTypes.ts
grep -n "quotation" frontend/src/lib/fieldLabels.ts frontend/src/lib/docTypes.ts

# Verify test file exists and covers both doc types
grep -n "tender_rfq\|quotation" frontend/src/components/LineItemsTable.test.tsx
```
</verification>

<success_criteria>
- LINE_ITEM_KEYS has 5 entries (was 3) — includes tender_rfq and quotation
- DOC_TYPES_WITH_LINE_ITEMS has 5 entries (was 3) — includes tender_rfq and quotation
- LineItemsTable.test.tsx exists with 4 test cases covering P6-FE-01 and P6-FE-02
- Frontend vitest suite passes with 0 failures (non-vacuous — tests assert specific constant values)
- No new components created (existing LineItemsTable handles all doc types)
</success_criteria>

<output>
After completion, create `.planning/phases/06-the-tables-of-requests-offers-and-quotations-must-be-extracted-these-tables-contain-the-requested-products-this-information-must-be-extracted-in-addition-purchase-orders-or-order-notes-also-describe-the-awarded-products/06-02-SUMMARY.md`
</output>
