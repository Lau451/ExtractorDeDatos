---
phase: 05-web-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/lib/utils.ts
  - frontend/src/lib/api.ts
  - frontend/src/lib/errorMessages.ts
  - frontend/src/lib/statusText.ts
  - frontend/src/lib/fieldLabels.ts
  - frontend/src/lib/docTypes.ts
  - frontend/src/types/api.ts
  - frontend/src/test-setup.ts
  - frontend/components.json
  - frontend/src/components/ui/table.tsx
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/ui/input.tsx
  - frontend/src/components/ui/select.tsx
  - frontend/src/components/ui/badge.tsx
autonomous: true
requirements:
  - REV-01
  - REV-02
  - REV-03
  - REV-04

must_haves:
  truths:
    - "Frontend project builds without errors via npm run build"
    - "Vite dev server starts and proxies /api/* to localhost:8000"
    - "TypeScript types match the actual JobResponse shape from the backend"
    - "All five API endpoints have typed fetch wrappers"
    - "Field label mappings exist for all extraction schema fields across all 5 doc types"
  artifacts:
    - path: "frontend/package.json"
      provides: "Project manifest with all dependencies"
      contains: "react-dropzone"
    - path: "frontend/vite.config.ts"
      provides: "Vite config with proxy and Tailwind plugin"
      contains: "/api"
    - path: "frontend/src/lib/api.ts"
      provides: "Typed API wrappers for all 5 endpoints"
      exports: "api"
    - path: "frontend/src/types/api.ts"
      provides: "TypeScript interfaces matching backend models"
      contains: "JobResponse"
    - path: "frontend/src/lib/fieldLabels.ts"
      provides: "Human-readable labels for all extraction fields"
      contains: "po_number"
  key_links:
    - from: "frontend/vite.config.ts"
      to: "http://localhost:8000"
      via: "server.proxy config"
      pattern: "proxy.*api.*8000"
    - from: "frontend/src/lib/api.ts"
      to: "frontend/src/types/api.ts"
      via: "import JobResponse"
      pattern: "import.*JobResponse.*types"
---

<objective>
Scaffold the React + Vite + Tailwind + shadcn/ui frontend project with all foundation code: TypeScript types, API client layer, utility libraries, field label registry, and test infrastructure.

Purpose: Establish the complete frontend foundation so that Plans 02 and 03 can build components without any setup work. Every type, utility, and API wrapper is ready to import.
Output: A building `frontend/` directory with all non-component code complete.
</objective>

<execution_context>
@C:/Users/LAUREANO/OneDrive/Escritorio/ExtraccionDeDatosNE2.0/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/LAUREANO/OneDrive/Escritorio/ExtraccionDeDatosNE2.0/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-web-ui/05-CONTEXT.md
@.planning/phases/05-web-ui/05-RESEARCH.md
@.planning/phases/05-web-ui/05-UI-SPEC.md

<interfaces>
<!-- Backend models the frontend must match -->

From src/api/models.py:
```python
class JobResponse(BaseModel):
    job_id: str
    status: str
    result: Optional[JobResultPayload] = None
    doc_type: Optional[str] = None
    extraction_result: Optional[dict] = None
    error_code: Optional[str] = None
    error_message: Optional[str] = None

class DocTypeOverrideRequest(BaseModel):
    doc_type: str

class PatchFieldsRequest(BaseModel):
    fields: dict
```

From src/core/errors.py:
```python
DOCLING_TIMEOUT = "DOCLING_TIMEOUT"
DOCLING_PARSE_ERROR = "DOCLING_PARSE_ERROR"
GEMINI_ERROR = "GEMINI_ERROR"
INVALID_FILE_TYPE = "INVALID_FILE_TYPE"
FILE_TOO_LARGE = "FILE_TOO_LARGE"
```

Backend routes (no /api prefix — Vite proxy strips it):
- POST /extract — accepts multipart file, returns {job_id, status}
- GET /jobs/{id} — returns JobResponse (always HTTP 200, even on error)
- PATCH /jobs/{id}/doc_type — accepts {doc_type}, returns 202
- PATCH /jobs/{id}/fields — accepts {fields: {...}}, returns full JobResponse
- GET /jobs/{id}/export — returns CSV bytes with Content-Disposition: attachment

Extraction schema fields (for fieldLabels.ts):

PurchaseOrderResult header: po_number, issue_date, buyer_name, supplier_name, delivery_date, currency, total_amount, payment_terms, shipping_address, notes
PurchaseOrderResult line_items (POLineItem): item_number, description, sku, quantity, unit, unit_price, extended_price

InvoiceResult header: invoice_number, invoice_date, due_date, issuer_name, issuer_address, recipient_name, recipient_address, currency, subtotal, tax_total, total_amount, payment_terms, po_reference
InvoiceResult line_items (InvoiceLineItem): item_number, description, quantity, unit, unit_price, extended_price

TenderRFQResult (no line_items): tender_reference, issue_date, issuing_organization, submission_deadline, contact_person, project_title, currency, notes

QuotationResult (no line_items): quote_number, quote_date, vendor_name, vendor_address, buyer_name, valid_until, currency, subtotal, tax_total, grand_total, payment_terms, delivery_terms

SupplierComparisonResult header: project_name, comparison_date, rfq_reference, evaluation_criteria, recommended_supplier, notes
SupplierComparisonResult line_items (SupplierRow): supplier_name, item_description, unit_price, total_price, lead_time, payment_terms, delivery_terms, warranty, compliance_notes, overall_score
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite project, install all dependencies, initialize shadcn/ui and components</name>
  <files>
    frontend/package.json
    frontend/vite.config.ts
    frontend/tsconfig.json
    frontend/tsconfig.app.json
    frontend/tsconfig.node.json
    frontend/index.html
    frontend/src/main.tsx
    frontend/src/App.tsx
    frontend/src/index.css
    frontend/src/lib/utils.ts
    frontend/src/test-setup.ts
    frontend/components.json
    frontend/src/components/ui/table.tsx
    frontend/src/components/ui/button.tsx
    frontend/src/components/ui/input.tsx
    frontend/src/components/ui/select.tsx
    frontend/src/components/ui/badge.tsx
  </files>
  <read_first>
    .planning/phases/05-web-ui/05-RESEARCH.md
    .planning/phases/05-web-ui/05-UI-SPEC.md
    .planning/phases/05-web-ui/05-CONTEXT.md
  </read_first>
  <action>
1. Run `npm create vite@latest frontend -- --template react-ts` from the repo root.
2. `cd frontend && npm install`
3. Install Tailwind v4: `npm install tailwindcss @tailwindcss/vite`
4. Install supporting libs: `npm install react-dropzone lucide-react clsx tailwind-merge class-variance-authority`
5. Install test deps: `npm install --save-dev vitest @testing-library/react @testing-library/jest-dom jsdom`

6. Replace `frontend/vite.config.ts` with:
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
  plugins: [react(), tailwindcss()],
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, ''),
      },
    },
  },
  resolve: {
    alias: { '@': '/src' },
  },
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: './src/test-setup.ts',
  },
});
```

7. Create `frontend/src/test-setup.ts`:
```typescript
import '@testing-library/jest-dom';
```

8. Replace `frontend/src/index.css` with:
```css
@import "tailwindcss";
```

9. Update `frontend/index.html` to add IBM Plex fonts in the `<head>`:
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;600&family=IBM+Plex+Sans:wght@400&display=swap" rel="stylesheet">
```

10. Initialize shadcn: `npx shadcn@latest init` — accept defaults (neutral palette).
11. Add components: `npx shadcn@latest add table button input select badge`

12. Create `frontend/src/lib/utils.ts` (standard shadcn cn helper):
```typescript
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

13. Replace `frontend/src/App.tsx` with a minimal placeholder:
```typescript
function App() {
  return <div className="min-h-screen bg-background font-sans">DocExtract</div>;
}
export default App;
```

14. Ensure `frontend/src/main.tsx` imports `./index.css`.

15. Run `npm run build` to verify the scaffold compiles.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json contains "react-dropzone"
    - frontend/package.json contains "lucide-react"
    - frontend/package.json contains "tailwindcss"
    - frontend/package.json contains "vitest"
    - frontend/package.json contains "@testing-library/react"
    - frontend/vite.config.ts contains "proxy"
    - frontend/vite.config.ts contains "localhost:8000"
    - frontend/vite.config.ts contains "tailwindcss"
    - frontend/vite.config.ts contains "test:"
    - frontend/src/index.css contains "@import"
    - frontend/index.html contains "IBM+Plex"
    - frontend/src/components/ui/table.tsx exists
    - frontend/src/components/ui/button.tsx exists
    - frontend/src/components/ui/input.tsx exists
    - frontend/src/components/ui/select.tsx exists
    - frontend/src/components/ui/badge.tsx exists
    - frontend/src/test-setup.ts contains "@testing-library/jest-dom"
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>Vite project scaffolded with React 19, Tailwind v4, shadcn/ui (5 components installed), all runtime and test dependencies present, IBM Plex fonts loaded, Vite proxy configured, build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create TypeScript types, API client, and all utility modules</name>
  <files>
    frontend/src/types/api.ts
    frontend/src/lib/api.ts
    frontend/src/lib/errorMessages.ts
    frontend/src/lib/statusText.ts
    frontend/src/lib/fieldLabels.ts
    frontend/src/lib/docTypes.ts
  </files>
  <read_first>
    src/api/models.py
    src/core/errors.py
    src/extraction/schemas/purchase_order.py
    src/extraction/schemas/invoice.py
    src/extraction/schemas/tender_rfq.py
    src/extraction/schemas/quotation.py
    src/extraction/schemas/supplier_comparison.py
    .planning/phases/05-web-ui/05-RESEARCH.md
  </read_first>
  <action>
1. Create `frontend/src/types/api.ts`:
```typescript
export interface ExtractResponse {
  job_id: string;
  status: string;
}

export interface JobResponse {
  job_id: string;
  status: 'pending' | 'processing' | 'classifying' | 'extracting' | 'complete' | 'error' | string;
  result: { raw_text: string } | null;
  doc_type: string | null;
  extraction_result: Record<string, unknown> | null;
  error_code: string | null;
  error_message: string | null;
}

export interface DocTypeOverrideRequest {
  doc_type: string;
}

export interface PatchFieldsRequest {
  fields: Record<string, unknown>;
}

export interface ErrorResponse {
  error: string;
  message: string;
}
```

2. Create `frontend/src/lib/api.ts`:
```typescript
import type { ExtractResponse, JobResponse } from '@/types/api';

export const api = {
  async postExtract(file: File): Promise<ExtractResponse> {
    const form = new FormData();
    form.append('file', file);
    const res = await fetch('/api/extract', { method: 'POST', body: form });
    if (!res.ok) {
      const err = await res.json();
      throw new Error(err.message || err.error || 'Upload failed');
    }
    return res.json();
  },

  async getJob(jobId: string): Promise<JobResponse> {
    const res = await fetch(`/api/jobs/${jobId}`);
    return res.json();
  },

  async patchDocType(jobId: string, docType: string): Promise<void> {
    const res = await fetch(`/api/jobs/${jobId}/doc_type`, {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ doc_type: docType }),
    });
    if (!res.ok) {
      const err = await res.json();
      throw new Error(err.message || err.error || 'Doc type update failed');
    }
  },

  async patchFields(jobId: string, fields: Record<string, unknown>): Promise<JobResponse> {
    const res = await fetch(`/api/jobs/${jobId}/fields`, {
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ fields }),
    });
    if (!res.ok) {
      const err = await res.json();
      throw new Error(err.message || err.error || 'Field update failed');
    }
    return res.json();
  },

  exportUrl(jobId: string): string {
    return `/api/jobs/${jobId}/export`;
  },
};
```

3. Create `frontend/src/lib/errorMessages.ts`:
```typescript
export const ERROR_MESSAGES: Record<string, string> = {
  DOCLING_TIMEOUT: 'The document took too long to parse. Try a smaller file.',
  DOCLING_PARSE_ERROR: 'The document could not be read. Check the file is not corrupted.',
  GEMINI_ERROR: 'Gemini failed to process this document. Try uploading again.',
  INVALID_FILE_TYPE: 'Unsupported file type. Supported: PDF, XLSX, XLS, PNG, JPG, HTML.',
  FILE_TOO_LARGE: 'File is too large to process.',
};

export function getErrorMessage(code: string | null): string {
  if (!code) return 'An unexpected error occurred.';
  return ERROR_MESSAGES[code] ?? `Processing failed (${code}).`;
}
```

4. Create `frontend/src/lib/statusText.ts`:
```typescript
export const STATUS_TEXT: Record<string, string> = {
  pending: 'Uploading...',
  processing: 'Parsing document...',
  classifying: 'Classifying document...',
  extracting: 'Extracting fields...',
};

export function getStatusText(status: string): string {
  return STATUS_TEXT[status] ?? 'Processing...';
}
```

5. Create `frontend/src/lib/docTypes.ts`:
```typescript
export const DOC_TYPES_WITH_LINE_ITEMS = new Set([
  'purchase_order',
  'invoice',
  'supplier_comparison',
]);

export const VALID_DOC_TYPES = [
  'purchase_order',
  'tender_rfq',
  'quotation',
  'invoice',
  'supplier_comparison',
] as const;

export type DocType = typeof VALID_DOC_TYPES[number];

export const DOC_TYPE_LABELS: Record<string, string> = {
  purchase_order: 'Purchase Order',
  tender_rfq: 'Tender / RFQ',
  quotation: 'Quotation',
  invoice: 'Invoice',
  supplier_comparison: 'Supplier Comparison',
};
```

6. Create `frontend/src/lib/fieldLabels.ts` — a complete mapping of every extraction schema field key to a human-readable label. The LINE_ITEM_KEY constant per doc type maps the key in extraction_result that holds the line items array:

```typescript
export const FIELD_LABELS: Record<string, string> = {
  // Purchase Order header
  po_number: 'PO Number',
  issue_date: 'Issue Date',
  buyer_name: 'Buyer',
  supplier_name: 'Supplier',
  delivery_date: 'Delivery Date',
  currency: 'Currency',
  total_amount: 'Total Amount',
  payment_terms: 'Payment Terms',
  shipping_address: 'Shipping Address',
  notes: 'Notes',

  // Invoice header
  invoice_number: 'Invoice Number',
  invoice_date: 'Invoice Date',
  due_date: 'Due Date',
  issuer_name: 'Issuer',
  issuer_address: 'Issuer Address',
  recipient_name: 'Recipient',
  recipient_address: 'Recipient Address',
  subtotal: 'Subtotal',
  tax_total: 'Tax Total',
  po_reference: 'PO Reference',

  // Tender/RFQ
  tender_reference: 'Tender Reference',
  issuing_organization: 'Issuing Organization',
  submission_deadline: 'Submission Deadline',
  contact_person: 'Contact Person',
  project_title: 'Project Title',

  // Quotation
  quote_number: 'Quote Number',
  quote_date: 'Quote Date',
  vendor_name: 'Vendor',
  vendor_address: 'Vendor Address',
  valid_until: 'Valid Until',
  grand_total: 'Grand Total',
  delivery_terms: 'Delivery Terms',

  // Supplier Comparison header
  project_name: 'Project Name',
  comparison_date: 'Comparison Date',
  rfq_reference: 'RFQ Reference',
  evaluation_criteria: 'Evaluation Criteria',
  recommended_supplier: 'Recommended Supplier',

  // Line item fields (shared across doc types)
  item_number: 'Item #',
  description: 'Description',
  sku: 'SKU',
  quantity: 'Quantity',
  unit: 'Unit',
  unit_price: 'Unit Price',
  extended_price: 'Extended Price',
  item_description: 'Item Description',
  total_price: 'Total Price',
  lead_time: 'Lead Time',
  warranty: 'Warranty',
  compliance_notes: 'Compliance Notes',
  overall_score: 'Overall Score',
};

/** Fallback: convert snake_case to Title Case */
export function getFieldLabel(key: string): string {
  return FIELD_LABELS[key] ?? key.replace(/_/g, ' ').replace(/\b\w/g, (c) => c.toUpperCase());
}

/** The key within extraction_result that holds the line items array, per doc type */
export const LINE_ITEM_KEYS: Record<string, string> = {
  purchase_order: 'line_items',
  invoice: 'line_items',
  supplier_comparison: 'line_items',
};
```

7. Run `cd frontend && npm run build` to verify everything compiles.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/types/api.ts contains "export interface JobResponse"
    - frontend/src/types/api.ts contains "extraction_result"
    - frontend/src/types/api.ts contains "error_code"
    - frontend/src/lib/api.ts contains "async postExtract"
    - frontend/src/lib/api.ts contains "async getJob"
    - frontend/src/lib/api.ts contains "async patchDocType"
    - frontend/src/lib/api.ts contains "async patchFields"
    - frontend/src/lib/api.ts contains "exportUrl"
    - frontend/src/lib/api.ts contains "/api/extract"
    - frontend/src/lib/api.ts contains "/api/jobs/"
    - frontend/src/lib/errorMessages.ts contains "DOCLING_TIMEOUT"
    - frontend/src/lib/errorMessages.ts contains "GEMINI_ERROR"
    - frontend/src/lib/errorMessages.ts contains "getErrorMessage"
    - frontend/src/lib/statusText.ts contains "Uploading..."
    - frontend/src/lib/statusText.ts contains "Classifying document..."
    - frontend/src/lib/statusText.ts contains "Extracting fields..."
    - frontend/src/lib/fieldLabels.ts contains "po_number"
    - frontend/src/lib/fieldLabels.ts contains "invoice_number"
    - frontend/src/lib/fieldLabels.ts contains "tender_reference"
    - frontend/src/lib/fieldLabels.ts contains "quote_number"
    - frontend/src/lib/fieldLabels.ts contains "project_name"
    - frontend/src/lib/fieldLabels.ts contains "LINE_ITEM_KEYS"
    - frontend/src/lib/fieldLabels.ts contains "getFieldLabel"
    - frontend/src/lib/docTypes.ts contains "DOC_TYPES_WITH_LINE_ITEMS"
    - frontend/src/lib/docTypes.ts contains "DOC_TYPE_LABELS"
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All TypeScript types match backend models exactly. API client covers all 5 endpoints with typed wrappers. Error messages, status text, field labels (covering all 5 doc types with 40+ field keys), and doc type constants are complete. TypeScript compiles with zero errors.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` exits 0
- `cd frontend && npx tsc --noEmit` exits 0
- All 6 lib files exist and export expected symbols
- Vite config has proxy, Tailwind plugin, test config, and @ alias
</verification>

<success_criteria>
- Frontend scaffold builds successfully
- All shadcn/ui components (table, button, input, select, badge) are installed
- TypeScript types match backend JobResponse shape exactly
- API client has typed wrappers for all 5 backend endpoints
- Field labels cover every field in all 5 extraction schemas
- Vitest is configured and ready for component tests in Plans 02/03
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-ui/05-01-SUMMARY.md`
</output>
